---
status: active
project: personal
type: log
---

# Wednesday, August 19, 2026

## Index
- **Barehands board — gold hologram coin** — staged/re-staged the gold hologram coin (`holo/GoldCoin.glb`) on the glass board multiple times through the session; unresolved question on Halloween plans left open.
- **Built out Momentum Signal Suite (TradingView Pine indicator) across multiple rounds** — added volume/EMA-close/zone-distance filters, dynamic trendlines, order blocks, and a real rolling POC; hit and fixed a signal-starvation problem twice by making every filter independently toggleable; new [[Momentum Signal Suite]] note documents the build and the input-index map for programmatic tuning.

## Session 1 — evening: Barehands board coin staging
### What Got Done
- Confirmed `holo/GoldCoin.glb` and `holo/Coin.glb` exist in the media airlock (`~/barehands/media/holo/`).
- Staged the gold hologram coin on the board repeatedly via `board.sh add_img`, checking board state each time before re-staging to confirm current state rather than assuming.

### What's Still In Progress
- Boss said "I'm going to do the Halloween" — asked for clarification (costume/party/decorations/other), no answer given yet.

### Decisions Made
-

### Notes Touched
-

### Profile Updates
-

## Session 2 — Momentum Signal Suite: built out with real confluence layers
### What Got Done
- Adam asked to add three filters to "the indicator we built on TradingView" — found it via `pine_list_scripts` (most-recently-modified script that session, "Momentum Signal Suite"): volume/tick-volume confirmation, close-beyond-Fast-EMA (not just a wick touch), and minimum distance from the nearest S/R zone. Reviewed the real source first, proposed the exact diff, got a yes, pushed via `tradingview-mcp`'s `pine_set_source` + `pine_smart_compile`, verified clean compile + live screenshot.
- Adam linked a marketing page (zynalgo.com, "Scalper Pro V4") claiming trendline-related features. Fetched it and called it out plainly: fake live-visitor counter, fabricated "trusted by Jane Street/Citadel/etc" logos with a buried disclaimer, scarcity countdown, no real methodology — the "trendline" mention was only in the pain-point marketing copy, not a real feature. No spec taken from it.
- Adam then showed a second, different page (zynalgo.net, "Trendline" product) via phone screenshots — same funnel-mill operation, but with real, buildable feature bullets (range S/R zones, breakout alerts, false-breakout filter, trend-following momentum entries). Used those as inspiration, not as copied logic (paid/closed-source, nothing to reverse-engineer): built genuine swing-to-swing dynamic trendlines (native TF pivot highs/lows), close-confirmed breakout with an ATR margin, drawn live with their own independent "TL BREAK" label — deliberately NOT stacked as a required gate on the main signal by default (flagged the stacking-filter risk explicitly), added a toggle instead.
- Adam sent phone screenshots of his TradingView mobile view (dashboard covering the whole screen, "hardly any signals") plus reference screenshots from an ICT/SMC course (Currency Pros) showing a compact checklist dashboard, POC/volume-profile, BOS/liquidity-sweep labels, and order-block trading. Adam: wants order blocks (his preferred trade type), says POC is important, dashboard needs to shrink, signals need to fire more.
- Built and pushed a second round: independent on/off toggles for the volume/EMA-close/zone-distance filters plus loosened defaults, forced tiny table text (root cause of the oversized dashboard — it was never sized down), kept last-3 HTF zones per side with TF+price labels instead of overwriting to one, real order-block detection (displacement candle breaking structure, prior opposite candle boxed), and a real rolling volume-profile POC (100-bar lookback, 24 buckets, highest-volume row). Caught a real bug from Adam's own screenshot before he flagged it: "TL BREAK" labels were firing every bar price stayed past the line rather than once — fixed with an edge-trigger.
- Adam confirmed wiring OB confluence and POC-side into the actual signal gating (not just visual). Did it, but had to fix two real architecture issues while building it: (1) OB confluence was about to be silently coupled to the "Show Order Blocks" display toggle — split box-tracking values from the visual box object so display and logic are independent; (2) POC was only computed on the last bar (fine for display, useless for gating/backtesting since every historical bar would see no POC) — moved the calculation to run every bar, kept only the drawn line/label last-bar-only for performance.
- Result: signal count went to zero with everything stacked (9 total AND conditions). Diagnosed live by toggling `in_26` (`useOBFilter`) off via `indicator_set_inputs` — confirmed via screenshot that a new signal fired immediately after. Left `usePOCFilter` on (not the bottleneck). Adam said leave settings as-is for now.
- Wrote up the full build, the input-index map (`in_0`–`in_34`, confirmed against the live indicator), and the tuning lesson in new [[Momentum Signal Suite]] note; indexed in [[Forex Trading]].

- **Added trade-state tracking after a real read-the-chart problem.** Adam asked how to read a USDCHF 15m setup off the dashboard. Called it honestly: the "trade" shown (BUY, entry 0.79535, TP2 0.79657) was long dead — price was already ~40 pips past TP2 — because the dashboard stored last-signal levels and had no concept of a trade ending, so every resolved signal read as live forever. Built a `Status` row (ACTIVE / TP1 HIT / TP2 HIT / STOPPED / --) plus auto-dimming of the Entry/SL/TP block once resolved. SL checked before TP so a bar sweeping both counts as a loss, not a win; signal bar stamped so a trade can't resolve on its own entry candle. Adam confirmed live on his phone ("yea it's grey out").
- Chart read given alongside it: price broke the red descending trendline (legit TL BREAK, closed clear), rallied ~0.80050, rejected, and came back to test POC 0.79920 from above — flagged as a retest decision point, not an entry, and specifically warned the green "support" trendline is sloping DOWN (drawn off consecutive lower lows), so it's not real support and can never fire a breakdown signal (requires positive slope).

### What's Still In Progress
- OB and POC gating are live but unwatched — no real market hours have passed to judge actual signal quality/frequency with the current settings (OB filter off, POC filter on).
- No formal backtest/forward-test run on the full confluence stack yet.
- USDCHF 15m: price testing POC 0.79920 from above after the trendline break — unresolved (holds = continuation, closes below = failed breakout).
- A cross-device gotcha worth knowing: Adam's phone kept rendering a pre-trendline cached copy of the script for a while after desktop saves. Diagnosed by dashboard row count (7 rows = original, 9 = current), not guesswork. It caught up on its own; if it recurs, remove/re-add the indicator or restart the app.

### Decisions Made
- Every new signal filter gets its own independent toggle by default going forward — stacking hard-AND'd conditions without an escape hatch caused two separate signal-starvation incidents in one session.
- Zynalgo (both `.com` and `.net`) treated as marketing-only source material, never as a spec to copy — same operator, same funnel template, no real methodology ever published.

### Notes Touched
- [[Momentum Signal Suite]] (new), [[Forex Trading]] (index entry added)

### Profile Updates
- None.
