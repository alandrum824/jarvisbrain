---
status: active
project: personal
type: log
---
# Monday, August 31, 2026

## Index

- **TWP ORB EA risk review** — found a real $35.70 gold-leg loss (10-hour hold, EA's own time exit, not a stop-loss hit), decoded the deployed config's real risk profile, and confirmed the vault already showed Adam had pulled gold/GBPJPY back to EURUSD-only.

## Session 1 — 11:24 PM: TWP ORB EA risk review, gold config decoded

### What Got Done
- Pulled live account 8996721 state via the MT5 bridge: balance $371.89, no open positions, no trades since 08-28 until one XAUUSD cycle on 08-31 (opened 05:35 UTC, closed 15:35 UTC, -$35.70).
- Checked the economic calendar — no high-impact USD news in that window, so the loss wasn't news-driven.
- Found `TWP24 EURUSD Sets.md` already had a newer account of this from Adam directly: he pulled GBPJPY (his own call, before bed) and gold (after this -$35.70 loss plus a second -$7.29 loss on 09-01, manually closed from mobile) — only EURUSD America 3x3 is live now.
- Decoded the actual deployed gold config (`XAUUSD Breakout Asia 0.01.set`, magic 900790009): real ATR-based stop (`iSLMode=2`, 1x ATR(200,M15)), 8:1 TP:SL ratio, closed by its own 10-hour `iMaxHolding` timer (not a stop-loss hit), and `iUseAdvancedRisk` (the 4%/8% drawdown breaker) was off on this file.
- Tried to verify empirically via the Strategy Tester — staged the exact live config into the terminal's tester cache and ran it twice. First run hung (flat CPU, matches the EA's known hang bug); second run never spawned a tester process. Killed the hung process (tester-only, no capital at risk) and restored the terminal's original cached config both times.

### What's Still In Progress
- Whether/how gold gets redeployed — tightened `iSL`, shorter `iMaxHolding`, or `iUseAdvancedRisk` turned on with a real cap. Not decided.
- Strategy Tester on this EA is proving unreliable (2 for 2 failures tonight) — no fix attempted, just documented as a known limitation.

### Decisions Made
- None new tonight — Adam's decision to pull gold/GBPJPY was already made and recorded before this session; this session just surfaced and documented the real mechanism behind the loss.

### Notes Touched
- [[TWP24 EURUSD Sets]] — added the decoded gold-config risk profile and the failed tester attempts.
- [[Build TWP Set Files]] — updated the EA-hang lesson: not grid-specific, hit a non-grid config too.
- [[Active Priorities]] — closed the stale "verify all three legs" item, added the gold-config finding as a new open item.

### Profile Updates
-
