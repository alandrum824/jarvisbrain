---
status: active
project: personal
type: log
---

# Saturday, September 19, 2026

## Index

- **Base44 connected, PolyEdge rebuilt from scratch** — new Base44 app built over two builder turns after the first produced entities but no UI; three real defects found by code review and live API testing, all fixed by hand.
- **Proved the old PolyEdge's "edges" were model errors** — the +17pt Jaguars lean measured against two independent markets that agree within 1.3 points.
- **Banked the ESPN and Polymarket API traps** — moneyline path, silent-empty injuries endpoint, resolved-market trap, and the slug method now verified 15/15.

## Session 1 — 1:00 AM: PolyEdge rebuilt on Base44

### What Got Done
- **Base44 MCP came online** after Adam ran `/mcp reconnect all` (Zapier had needed the same earlier). Read the server README first as it requires.
- **Created a fresh Base44 app, PolyEdge** (`6aae5695dbc22f6715196ad4`) rather than upgrading [[RetroVault (CeeloEdge)]] — Adam's call: "fresh build premium hot new future looking." No existing Base44 app matched "poly."
- **Turn 1 reported `ready` but had built no UI at all** — entities existed, `App.jsx` was the untouched scaffold. Caught by reading the code rather than trusting the status. Turn 2, re-sent with the verified ESPN JSON path and a worked devig example, built the real app: five screens, real components, four backend functions.
- **Fixed three real defects by hand** (checkpoints saved before and after): the Polymarket integration was dead on arrival (wrong endpoint, returned political markets), the sigmoid was unclamped (would display a 100% pick), and a set of abbreviation aliases **I had guessed** would have broken four working lookups. Polymarket matching went from broken to **15/15 games**.
- **Verified every data dependency live** rather than assuming: ESPN moneylines (both sides, all 15 MLB games, vig 4.5–4.8%), the same path on NFL and college football, probable starting pitchers, the injuries endpoint, and Polymarket prices.
- **Built a reference implementation** (`polyedge_ref.py`) to check the app's numbers against. Its own self-check caught the float64 sigmoid bug, and it demonstrated that a null 50% model still emits 9 picks from 15 games — proof the pass gate must key on data completeness first, gap size second.
- **Searched for free GitHub/MCP options** at Adam's request and recommended against the leading one (`sportsdata-mcp`, 16 stars, and since v0.31.0 it can place real bets). The data it offers we already pull directly with no dependency.

### What's Still In Progress
- **The app has not been driven in a browser yet.** Code is verified, the live UI is not. Nothing deployed; preview only.
- **Most fragile dependency: ESPN blocks the Base44 Worker's egress (403)**, so the app falls back through free public CORS proxies. Adam has not been asked about the optional `SCRAPER_API_KEY` that would make this solid.
- The Odds API key — Adam mentioned having a free one. Not yet entered; it must go in as a server-side Base44 secret by him, never in frontend code. The app runs ESPN-only without it.
- `polyedge_ref.py` still lives in the session scratchpad; needs a permanent home if it stays useful.

### Decisions Made
- Fresh Base44 app rather than upgrading RetroVault, and no third-party sports MCP server installed — the free ESPN and Polymarket APIs are called directly.
- **Verify, never guess, applies to my own work too** — the abbreviation aliases I invented were caught only because they were tested against live slugs before shipping. Two of seven were real.
- Base44's `ready` status is not evidence that anything was built; read the code.

### Notes Touched
- [[PolyEdge]] — added the Jaguars proof, the full build log, the three defects, and the ESPN-egress constraint.
- [[Public ESPN API]] — added the exact moneyline JSON path (the obvious field is always null), the odds-timing constraint, and the silent-empty injuries endpoint.
- [[Polymarket (Prediction Markets)]] — added the resolved-market trap, both dead-end browse endpoints, the verified slug method, the `sportsMarketType` discriminator, and the User-Agent note.

### Profile Updates
-
