---
status: active
project: personal
type: log
---
# Tuesday, August 25, 2026

## Index

- **Checked both Risen accounts, dug into a mystery trade, tightened TWI Scalp Pro to NY-only.** Confirmed mom's account is riding a real TWP Robot drawdown; traced a confusing -$17.04 trade to a real timezone-display gap between the phone (broker/server time) and this VPS's system clock — Adam found the exact trade himself (TWI Scalp Pro London, stopped out). Adam disabled the London session on RisenAdam himself; RisenMOM's still live on London by his choice.
- **Pulled real 14-day EA performance data: TWI Sniper is outperforming TWI Scalp Pro.** 8W/0L, +$368.05 vs. 3W/1L, +$207.67, combined across both accounts — grouped by real entry comment/magic number, not the misleading SL/TP-price comment TradingView leaves on the closing deal.
- **Built and shipped TWI Weekly Bias, a new TradingView indicator.** A real ICT/SMC weekly liquidity-sweep dashboard (from a trading video Adam shared), built as a single clear verdict (BUY WEEK/SELL WEEK/LEAN/WAIT) backed by weekly sweep bias, daily bias, multi-timeframe structure, and a rolling POC. Hit a serious TradingView MCP tooling outage mid-build (every Pine Editor connector tool failed) and found a real, working fallback (clipboard-paste injection into Monaco) rather than giving up or reporting failure — compiled clean, live on chart, verified against real data.

## Session 1 — Morning: account checks, EA performance pull, TWI Weekly Bias build

### What Got Done
- Checked both Risen accounts on request. RisenAdam: $422.64, flat. RisenMOM (mom's demo/FTMO): balance moved $10,569→$10,441 across the session while a TWP Robot SELL sat open, floating between -$74 and -$30 — reported live each time, not logged per Adam's standing "no need to log" rule for that account.
- Investigated a specific -$17.04 trade Adam flagged from a phone screenshot. Initial API pulls came back empty on both accounts — traced this to a real ~5-hour display gap between the phone's shown time (broker/server time) and this VPS's system clock, not a wrong-account issue. Before fully resolving it via the API, Adam pulled up the trade's own detail screen and found the answer directly: ticket #8034471, TWI Scalp Pro (London session), stopped out for -$17.04 in under 14 minutes on RisenAdam.
- That, plus yesterday's -$53.84 London-session loss on RisenMOM, led Adam to decide: stick to NY session only for TWI Scalp Pro. Found the real toggle in the live `.mq5` source (`TradeLondon`/`TradeNewYork` bools). Adam made the change himself directly in the live Inputs dialog — confirmed he only did this on **RisenAdam**; RisenMOM is still running `TradeLondon = true` live by his explicit choice ("Not interested" in changing it further). Did not touch the `.mq5` source file's own default, per his call.
- Adam asked which TWI EA is actually performing better. Pulled real 14-day closed-deal history from both accounts, grouped correctly by entry comment + magic number (not by the closing deal's own comment, which MT5 overwrites with the SL/TP trigger price — a real gotcha caught and worked around). Result: **TWI Sniper 8W/0L, +$368.05** vs. **TWI Scalp Pro 3W/1L, +$207.67**, combined across both accounts — TWI Sniper cleanly ahead on both win rate and total profit.
- Adam asked whether an EA could be built to replicate a fast multi-trade basket-scalp pattern he was watching (almost certainly TWP Robot, a third-party EA already on the accounts). Confirmed it's buildable, flagged the real risk honestly (grid/basket scalpers can look great in chop and get hurt hard in a real trend run since TWP's actual risk logic is closed-source and can't be cloned, only the visible behavior), and asked for real risk parameters before building anything. Adam said "just curious" — parked, nothing built.
- Adam shared a series of trading-education video screenshots (SMC + Volume Profile, IR/OR range concept, then a weekly liquidity-sweep "Buy Week/Sell Week" directional bias dashboard). Read and explained each accurately from what was actually visible/labeled on screen rather than guessing, connected it to real, already-existing logic in Adam's own tools ([[TWI True North]], [[Malaysian SNR x Orderflow]], [[TWI Scalp POC]]).
- Adam asked to build it as a brand-new TradingView indicator, deferred scope to Jarvis's judgment with one instruction: "make it easy to understand." Designed [[TWI Weekly Bias]] as a single clear verdict dashboard rather than a wall of raw confluence data.
- Hit a real, serious TradingView MCP tooling failure mid-build: every Pine Editor connector tool (`pine_open`, `pine_set_source`, `pine_smart_compile`, `pine_get_source`, `pine_get_errors`) failed with "Could not open Pine Editor" / "Monaco not found in React fiber tree" across 10+ retries — well beyond the known 1-3-retry flakiness already banked from prior sessions. Diagnosed the real cause (the tool's React-fiber-walk detection method found zero fiber keys on this build) and built a genuine working fallback instead of stopping: duplicated a script via the UI directly, found Monaco's hidden input textarea via `ui_evaluate`, and injected the real source by dispatching a synthetic `ClipboardEvent('paste')` — base64-encoding the source first to dodge escaping issues. Verified the injection landed by reading back the rendered `.view-line` DOM text before compiling.
- Saved (Ctrl+S) and clicked "Add to chart" directly (bypassing the broken `pine_smart_compile`) — compiled clean, 0 errors, confirmed live on chart via `chart_get_state`, and pulled real dashboard output via `data_get_pine_tables` to confirm it was producing a coherent, sane read on live GBPJPY data (a LEAN SELL call correctly reflecting weekly/daily/H4 disagreement) rather than just trusting a clean attach.
- Banked the full working fallback method in [[Malaysian SNR x Orderflow]] (the existing home for Pine Editor tooling gotchas) so a future session doesn't have to rediscover it, and created a dedicated [[TWI Weekly Bias]] note documenting the indicator's real logic and open items.

### What's Still In Progress
- RisenMOM's TWP Robot position was still open and moving at last check.
- TWI Weekly Bias has zero backtesting — one live snapshot confirmed sane, not validated over any real sample.
- The super-scalper EA idea is parked, no design started.

### Decisions Made
- TWI Scalp Pro restricted to NY session only on RisenAdam (Adam made the change himself). RisenMOM stays on both sessions, Adam's explicit call.
- Building the new "buy week/sell week" concept as a brand-new indicator ([[TWI Weekly Bias]]), not folded into [[TWI True North]].

### Notes Touched
- [[Malaysian SNR x Orderflow]] (new Pine Editor tooling-outage gotcha + fallback method banked), [[TWI Weekly Bias]] (new), [[Forex Trading]] (index entry added), [[TWI Scalp Pro]] (session-toggle finding, not yet written up there — see Open Questions below)

### Profile Updates
- None.

## Session 2 — v2 fix: order blocks, buy/sell signals, compact dashboard

### What Got Done
- Adam tested v1 of [[TWI Weekly Bias]] and came back with direct feedback: no order blocks plotted, no buy/sell signals, dashboard too big for mobile. Rebuilt the indicator same day (v2): added real order-block box plotting (backward-scan to the last opposite-colored candle before a structure break), added green/red BUY/SELL `plotshape` triangles tied to local structure breaks (gated to agree with the weekly bias by default), and added a `compactDash` toggle that collapses the dashboard from 6 rows to 2 for mobile.
- The normal Pine Editor tool path (`pine_set_source`/`pine_smart_compile`) was still down from earlier in the day, so injected v2 through the same clipboard-paste fallback banked in [[Malaysian SNR x Orderflow]]. v2's source has non-ASCII arrow glyphs (▲▼–), which plain `atob()` (used successfully for v1) would have corrupted — switched to UTF-8-safe decoding (`TextDecoder('utf-8')`) for this injection, confirmed it worked (dashboard rendered the arrow glyphs correctly, not garbled).
- Verified v2 live on FX:GBPJPY: 33 order block boxes drawn and visible, green BUY triangles firing, dashboard compacted to exactly 2 rows (`WkBias | LEAN SELL` / `Detail | W▼ D▲ H4▲ L▲`) — confirmed via `data_get_pine_boxes`, `data_get_pine_tables`, and a chart screenshot, not just a clean compile.
- Updated [[TWI Weekly Bias]] with the full v2 changelog and a fresh open-items list.

### What's Still In Progress
- v2 still has zero backtesting — same open item as v1, now also covering the new buy/sell signals and order-block pivot length (untuned).

### Decisions Made
- None new this session — pure bugfix/rework on Adam's explicit feedback, no new trading decisions.

### Notes Touched
- [[TWI Weekly Bias]] (v2 changelog + open items added).

## Session 3 — TWI Sniper ORB: ATR-adaptive trail (v2.62)

### What Got Done
- Pulled a fresh EA win/loss record on request: combined 49W/25L across both accounts. Broken out — [[TWI Sniper ORB]] 10W/0L (+$375.56), [[TWI Scalp Pro]] 3W/5L (-$30.86, London-session losses concentrated on RisenMOM even after RisenAdam was cut to NY-only), TWP Robot 14W/7L (+~$68), and a still-unidentified magic=0/no-comment bucket on RisenAdam (22W/13L, -$322.69) flagged for future digging.
- Adam called TWI Sniper the best-performing EA and asked for real improvement ideas. Gave an honest read first: 10 live trades is thin, and the one real backtest (24 trades, 3 weeks) came out near-breakeven (profit factor ≈1.04), every exit closed by the trailing stop rather than a raw TP/SL — the live streak is likely the BE/trail management doing the work, not a dominant raw entry edge yet. Proposed testing the actual `ENTRY_SNIPER` retrace mode (the EA defaults to aggressive `ENTRY_BREAK`, not the retrace logic it's named for), making the trail volatility-adaptive (ATR-based) instead of one fixed number, running a proper large-sample backtest before scaling size, and a partial-close-at-1R option.
- Adam approved the ATR-trail change. Built it: new `InpUseATRTrail`/`InpATRPeriod`/`InpATRMultiplier` inputs, a new `atrHandle`/`ATRValue()` (mirrors the existing EMA handle pattern), and `ManageOpen()`'s trail block now uses `ATR × multiplier` instead of the flat `g_trail` distance — with an automatic fallback to the original fixed-points behavior if ATR is ever unavailable. Applied directly to the live file (`TWIORB24.mq5`, RisenAdam terminal), bumped to v2.62, compiled clean twice via MetaEditor CLI (0 errors, 0 warnings both times). Mirrored the same edit into the vault's own embedded source copy in [[TWI Sniper ORB]] so it stays the documented source of truth.
- **Real discovery made in the process:** RisenMOM is running a materially older build of this EA — v2.31 vs. RisenAdam's v2.61/2.62, missing the asset-profile system and `InpGoldTrail` entirely, about 600 lines shorter. Not previously documented. Real TWI Sniper trades are closing on RisenMOM under this old version (4W/0L this period). Left it untouched rather than patch a different codebase blind — flagged for Adam to decide whether to bring it up to date.

- Adam said yes to upgrading RisenMOM too. Rather than hand-patch the older v2.31 codebase, copied RisenAdam's v2.62 file over RisenMOM's outright (full replacement, both terminals now byte-identical). Compiled clean via RisenMOM's own MetaEditor CLI: 0 errors, 0 warnings.

### What's Still In Progress
- **Neither account has actually loaded the new logic yet** — MT5 doesn't hot-reload a recompiled `.ex5` on an already-attached chart. Adam needs to remove and re-attach the EA on both RisenAdam and RisenMOM (both accounts show 0 open positions right now, safe moment to do it) and confirm the new `InpUseATRTrail`/`InpATRPeriod`/`InpATRMultiplier` inputs appear in each Properties dialog.
- Not independently verified what RisenMOM's live chart had customized (lot mode/size, etc.) before this change — worth Adam double-checking the full Inputs list after reattaching on both terminals rather than assuming everything carried over as intended, since several input names were repurposed between v2.31 and v2.62.
- The unidentified magic=0 bucket on RisenAdam (-$322.69, 22W/13L) is still undiagnosed.
- `ENTRY_SNIPER` A/B backtest and the larger-sample backtest are proposed but not started — same live-terminal constraint as before (headless Strategy Tester won't run against an already-live terminal, and there's no MT5 UI automation tool to drive Strategy Tester manually).

### Decisions Made
- Build the ATR-based adaptive trail for TWI Sniper ORB (approved, built, compiled on both accounts — not yet live-activated on either).
- Upgrade RisenMOM from its old v2.31 build to the current v2.62 (approved, done, compiled clean).

### Notes Touched
- [[TWI Sniper ORB]] (v2.62 changelog, updated embedded source, RisenMOM version-mismatch discovery documented).
